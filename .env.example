DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=mydb

# LLM provider: "gemini" (default) or "anthropic"
LLM_PROVIDER=gemini

# Gemini (required only if LLM_PROVIDER=gemini)
GEMINI_API_KEY=

# Anthropic (required only if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=

# Optional: path to a .sql file describing your DB schema
SCHEMA_PATH=
